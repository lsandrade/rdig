<h1>New segment with criteria</h1>

<%= form_for(@segment) do |f| %>
  <% if @segment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@segment.errors.count, "error") %> prohibited this segment from being saved:</h2>

      <ul>
      <% @segment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, value:"Name your custom segment" %><br>
    <%= f.text_field :name, class:"form-control"%>
  </div>

  <div class="field">
    <h2>Criteria</h2>
    
    <button class="btn btn-success">New Criterium</button><br>
    <label for="campo">Field</label>
    <select name="campo" id="" class="form-control">
      <option value="1">Name</option>
    </select> -
    <label for="operador">Operator</label>
    <select name="operador" id="" class="form-control">
      <option value="1">Igual a</option>
    </select> -

    <label for="valor" class="form-control">Value</label>
    <input type="text" class="form-control">
  
    <br><br>
   

  </div>

  <div class="actions">
    <%= f.submit "Send", type:"button", class:"btn btn-primary" %>
  </div>
<% end %>
